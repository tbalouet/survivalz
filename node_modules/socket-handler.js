var io      = require('socket.io');

function init(server){
    io  = io.listen(server);

    io.sockets.on('connection', function (socket) {
        socket.on('cubeMoved', function (datas) {
            socket.broadcast.emit('socketMoveCube', datas);
        });
    });
}

exports.init = init;